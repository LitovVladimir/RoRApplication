<p id="notice"><%= notice %></p>

<h1 class="table_name">Просмотр записи</h1>

<div class="show_container">
  <table class="table_blur">
    <thead>
    <tr>
      <th>Книга</th>
      <th>Абонент</th>
      <th>Дата доставки</th>
      <th>Дата возврата</th>
      <th colspan="3"></th>
    </tr>
    </thead>

    <tbody>
        <tr>
          <td><%= @delivery.book_id %></td>
          <td><%= @delivery.subscriber_id %></td>
          <td><%= @delivery.delivery_date %></td>
          <td><%= @delivery.return_date %></td>
          <td><%= link_to 'Изменить', edit_delivery_path(@delivery) %></td>
        </tr>
    </tbody>
  </table>
</div>
<div><%= link_to "<button class = 'back_button' type='button'></button>".html_safe, deliveries_path %></div>